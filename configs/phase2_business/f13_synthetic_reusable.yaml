# F13 figure — business dataset (synthetic), content-block-reuse variant
# Event definition: content_block_reuse
#   For each single-turn request, each unique block that appeared in any
#   strictly earlier request forms one reuse event.
#   reuse_time = current.timestamp - last_seen_ts[block_id]
# Run: python scripts/generate_f13_business.py configs/phase2_business/f13_synthetic_reusable.yaml

trace_name: business_synthetic
input_file: data/synthetic/business_synthetic.jsonl
event_definition: content_block_reuse
block_size: 128
x_axis_max_minutes: 120
output_dir: outputs/phase2_business/f13_synthetic_reusable
figure_variant: content_block_reuse
note: "synthetic dataset — all requests are single-turn (no parent_chat_id)"
