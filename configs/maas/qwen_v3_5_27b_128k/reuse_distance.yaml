input_file: data/internal/qwen_v3_5_27b_128k/requests.jsonl
output_dir: outputs/maas/qwen_v3_5_27b_128k/reuse_distance
# block_size = 2048 to match vLLM-Ascend deployment block granularity.
#
# Deployment: Qwen3.5-27B, 910B4 × 8, max_model_len=131072
#   prefix cache + mamba-cache-mode=align forces block_size = 2048 tokens
#   available_cache_blocks ≈ 500  (benchmark-derived capacity boundary)
#   total KV cache ≈ 500 × 2048 ≈ 1,024,000 tokens
#
# Block consumption per request at this block_size:
#   28K  prompt → ~15 blocks   56K → ~28    120K → ~59    128K → ~62
#
# Safe concurrency estimates (30% reserve → budget ≈ 350 blocks):
#   28K: concurrency 16   56K: concurrency 8–12   128K: concurrency 4–5
block_size: 2048
available_cache_blocks: 500
trace_name: Qwen-V3.5-27B-128K
note: "V4 Module 1 — reuse distance (block_size=2048 chars ≈ vLLM 2048-token block)"
