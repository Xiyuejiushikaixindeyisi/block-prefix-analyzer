# F4 figure — GLM-4.7-HCMAAS (MaaS), content-prefix-reuse variant
# Hit definition: content_prefix_reuse_blocks
#   Contiguous prefix match from request start — equivalent to ideal
#   (infinite-capacity, same-model) vLLM APC hit count.
# Run: python scripts/generate_f4_business.py configs/maas/glm_4_7_hcmaas/f4_prefix.yaml

trace_name: glm_4_7_hcmaas
input_file: data/internal/glm_4_7_hcmaas/requests.jsonl
hit_metric: content_prefix_reuse
block_size: 128
bin_size_seconds: 60
output_dir: outputs/maas/glm_4_7_hcmaas/f4_prefix
figure_variant: content_prefix_reuse
note: "GLM-4.7-HCMAAS MaaS data — ideal prefix cache hit rate over time (9:00-11:00 window)"
